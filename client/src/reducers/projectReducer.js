const intialState = {
    projects: [],
    filter: null
}
export default (state = intialState, action) => {
    switch (action.type) {
        case 'FETCH_PROJECTS_SUCCESS':

        default:
            return state;

    }
}